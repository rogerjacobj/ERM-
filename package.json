  "scripts": {
    "start": "node backend/index.js",
    "install-all": "npm install && cd backend && npm install"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^7.0.0"
  }
}
